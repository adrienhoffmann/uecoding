Session notes for 2025-12-13

Goals:
- Verify ping wheel Ctrl/Alt+Left behavior across multi-monitor setups and DPI scaling.
- Ensure clickable area behavior (adjusted green rectangle) matches tunables and does not block ping wheel.
- Verify right-click path update works and does not persist old path coordinates.
- Remove/clean debug overlays after verification.

Quick checklist:
- [ ] PIE: Ctrl+Left opens ping wheel anywhere inside green clickable area (incl. outside visible minimap)
- [ ] PIE: Right-click updates path on each click (path should follow latest right-click)
- [ ] PIE: No leftover invisible blocking rectangle when moving PIE window across monitors
- [ ] Unit/Integration: Add an automated test (if feasible) to check ScreenPositionToWorld mapping for sample positions

Verification steps:
1. Open `coding.uproject` and run PIE windowed.
2. Enable debug overlays in HUD and set `bShowClickableAreaDebug = true`.
3. Move PIE window partly off-screen or to secondary monitor and Ctrl+click in the upper/outside area that previously failed.
4. Copy relevant logs from output window.

Notes:
- If you still observe blocked clicks, paste the `ScreenPositionToWorld` log lines and a screenshot of the debug overlay; I'll tighten tolerance and add an absolute vs local reprojection sanity check.

