Session notes for 2025-12-11

Summary of current status:
- Fixed major issue causing PingWheel position offsets on Ctrl+click/minimap.
- Changes implemented in `PlayerHUDWidget.cpp`, `PlayerHUDWidget.h`, `MOBAPlayerController.cpp`, added debug logging and a debug visual overlay.
- Logic fix: Use viewport coordinates (GetMousePosition) and manually compute top-left corner to center the widget. Avoid using SetPositionInViewport(...) with `bRemoveDPIScale=true` or alignment because GameViewportSubsystem forces anchors to (0,0), making `SetAlignmentInViewport` ineffective.
- Pushed branch: `feat/tomorrow-2025-12-10` to origin (commit: 23eac0e).

Files changed (summary):
- Source/coding/PlayerHUDWidget.cpp: position logic, debug overlay, TopLeft calc
- Source/coding/PlayerHUDWidget.h: added debug toggles (bShowPingWheelDebug, LastPingWheelDebugPos)
- Source/coding/MOBAPlayerController.cpp: use same viewport centering logic when creating ping wheel in controller flow
- Content/*: updated WBP assets while testing (WBP_PlayerHUD2, wbp_pingwheel)
- Content/mapmoba.umap

How to reproduce locally (manual):
1. Open project in Unreal Editor (UE_5.7). Launch `coding.uproject`.
2. Open editor log/console to see debug messages.
3. Play the map (PIE) or run the editor in windowed mode that matches your target monitor setup (multi-monitor setups matter).
4. Ctrl+click on the minimap; observe the PingWheel. Should appear centered on cursor.
5. Check the yellow debug message with MousePos, DesiredSize, TopLeft values (if `bShowPingWheelDebug` is true). The ping wheel's center should match MousePos.

If the ping wheel is still offset:
- Open `WBP_PingWheel` Blueprint and verify:
  - Root is Canvas Panel.
  - No render transform (Scale = 1,1 ; Translation = 0,0).
  - No ScaleBox or weird parent widget that changes scale.
  - Child widgets have no offsets/padding that moves visuals off-center.
- Temporarily replace `PingWheelClass` (in PlayerHUDWidget/PlayerHUD blueprint) with a simple test widget with a 64x64 red square to isolate blueprint-level transforms.
- Enable `bShowPingWheelDebug` in `PlayerHUDWidget` instance to draw debug messages and confirm values.
- Verify master DPI/Windows scaling isn't changing the viewport unexpectedly.

Tests to run tomorrow:
- Multi-monitor setups - test with game on monitor 2 and monitor 1 cursor positions.
- DPI scaled displays - try 125% scaling and verify TopLeft calc remains accurate.
- Different map resolutions and window sizes.

Remaining tasks & notes:
- Check and clean `WBP_PingWheel` Blueprint to ensure it doesn't have transform/anchor issues; prefer a single Canvas Panel and center child elements with offsets.
- Remove debug markers after validation.
- Add unit/integration tests (automation) if possible to validate cursor-to-widget mapping across DPI scales.

Command history used:
- git add -A
- git commit -m "Fix ping wheel positioning: use viewport coords & manual centering; add debug logging & visual markers; update MOBAPlayerController & PlayerHUDWidget"
- git push --set-upstream origin feat/tomorrow-2025-12-10

Reviewer instructions:
- Review `PlayerHUDWidget.cpp` and `MOBAPlayerController.cpp` changes to ensure the logic is consistent and not performing double DPI conversions.
- Verify that `SetPositionInViewport` is called with `bRemoveDPIScale=false` when using viewport coords, and the TopLeft offset is computed properly.

Session goals for tomorrow:
- Verify fixes on a physical/multi-monitor environment (the user's exact hardware).
- If misposition persists, instrument WBP PingWheel widget to print out its own render transform values and anchors at runtime.
- Implement final removal of debug markers once verified.


---

Notes: If you want, I can also create a PR with a more detailed description and attach screenshots/logs for review (just confirm PR name and description).